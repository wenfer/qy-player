# Changelog

## 未发布

### 新增
- **下次打开接着上次的音乐**：关掉应用前在听的那首歌会留在播放条上（曲名、
  时长、进度都在），**不会自动出声**——点一下播放键就从上次的位置继续。
  这条状态只是一条记录（歌 + 位置），不会出现在"播放历史"里。
- **窗口和模式会记住了**：关掉应用时是什么样，下次打开就是什么样——影视模式下
  的窗口大小/位置/是否最大化、音乐模式的竖窄屏、乃至精简浮窗，都会原样恢复。
  显示器拔掉或分辨率变小后，窗口会自动夹回看得见的位置，不会跑丢在屏幕外。
  音乐模式里手动改过的尺寸不记（每次都是标准竖窄屏）。
- **走 mpv 的音乐也有真频谱了**：APE/WMA/CUE 这类冷门格式、以及服务器转码的
  音源以前只有一根静音底线（mpv 0.32 没有实时频谱接口）。现在只要机器上有
  ffmpeg，系统会在起播后**后台**把它解一遍、算出频谱备用，播放时跟着进度跳动
  ——**音乐本身仍然由 mpv 播放**，播放方式一点没变。一首 4 分钟的歌大约占
  100KB 缓存，超预算按最旧的先清。机器上没有 ffmpeg 就自动不生效（不影响
  播放），也可以在设置 → 音乐里整体关掉（默认开）。
- **音乐模式改成竖窄屏**：切到音乐模式时主窗口原地变成竖屏（460×820，矮屏
  自动收窄到能放下），退出原样恢复。同一个窗口改尺寸，不是新开一个窗口——
  播放状态与实时频谱都在这个窗口里，零跨进程同步，老机也扛得住。竖屏下侧栏
  收成一列图标，把宽度让给封面与曲目。

### 变更
- **音乐列表只认「音乐来源」**：音乐列表/专辑/歌手/收藏/搜索全部只显示在
  「音乐媒体库」里添加的来源扫出的曲目。此前从影视来源扫出的音轨（拆分域之前
  的存量）会继续挂在音乐列表上，但那个来源在音乐模式里根本看不到、也无法管理
  ——现在两边一致了，这样的音轨不再显示、也不再可播（歌单里残留的旧曲目会
  明确提示"来源已不属于音乐库"）。
- **旧的音乐目录可以一键转回来**：音乐媒体库新增「其它来源」区，把属于影视域
  的目录/WebDAV 来源一键改成音乐来源——已扫描的音轨立即回到音乐列表，
  **不需要重新扫描**；反过来在影视模式也可以把音乐来源改回影视来源。
- **音乐不再进播放历史**：听歌不再往"播放历史"里塞记录，也不会出现在
  "继续观看"里——音频不是影视，进度只用于下次打开时恢复播放条。点同一首歌
  **从头开始播**（不再按曲目记忆播放位置）。向 Emby/Jellyfin 的进度回传
  **照常保留**，服务器上的"继续收听"跨设备续播不受影响。升级时会把历史与
  进度表里已有的音频记录清掉（影视记录一条不动）。
- **音乐模式进来就是曲目列表 + 播放控件**：默认视图从「专辑网格」改成「全部
  曲目」列表，tab 顺序同步调整为 全部曲目 / 专辑 / 歌手 / 收藏；有音乐在放时，
  播放控件（上一曲 / 播放暂停 / 下一曲 / 进度 / 歌词 / 收藏 / 精简）停靠在音乐
  窗口底部，成为音乐界面的常驻一条，而不是居中浮卡。

### 变更
- **播放条重新排版（四行）**：频谱 / 曲名+时间 / 进度条 / 按钮各占一行。此前
  进度条被夹在播放按钮右边，又窄又点不动，暂停时还顶掉频谱——现在进度条独占
  一行、可点可键盘（←/→ 快退快进、Home/End 到头尾），频谱与它同时显示。
  暂停不再让频谱消失：画面**冻结在最后一帧**，正常放着的时候也不会因为进度
  每走一秒就重置峰值。按钮一律 32×32 且不许压缩（竖窄屏里此前会被挤成椭圆），
  放不下就换行。
- **频谱区域加高 + 可随时关掉**：播放条里的频谱从 24px 抬到 32px（LED 分段
  在老机分辨率下也看得清）；控制条上多了一个频谱开关按钮，不想看的时候一点
  就收起来，选择会记住（下次打开还是关着的）。
- **频谱只留一个，改成经典弹跳柱**：播放音乐时不再同时出现两块频谱——音乐页
  顶部那块完整版已移除，只保留底部播放条的拾音器。柱状频谱换成经典频谱分析仪
  的弹跳手感：分段 LED 柱（底段琥珀 → 顶段红）随音乐起伏，顶端峰值帽在电平掉
  下去后缓慢下落再回底，而不是跟着瞬间归零。瀑布声谱图一并移除（老机上它
  最费 CPU，且选择持久化那一套随之删掉）。

### 修复
- **侧栏菜单点不动**：上一版"恢复音乐模式"的回填逻辑会在每次路由变化后重新
  执行，把任何页面跳转都在瞬间弹回音乐页——表现为菜单怎么点都没反应。现在
  启动恢复只跑一次，菜单恢复正常。
- **音乐模式只记住了窗口、没记住内容**：上次退出时在音乐模式，重启后窗口是
  竖屏、里面显示的却是影视首页。现在恢复音乐模式时会连它的默认页（音乐列表）
  一起恢复；通过链接直达的页面（热重载场景）不受影响。
- **音乐列表里 mp3 一直不显示时长**：扫描只认 FLAC 与 m4a 的时长，mp3（ID3 标签里
  只读了文字，没解析音频帧）与 APE 一律留空——库里 214 首有 67 首没有时长。现在
  mp3 读 Xing/Info 头里的总帧数（退化到 ID3 的 TLEN，再退化到"码率恒定"的文件大小
  估算），APE 读文件头的描述符；**重扫一次媒体库**即成批补齐。少数解析不出来的
  （没有 Xing 头的 VBR、标签特别大的）会在**播放过一次之后自动补上**——播放器本来
  就知道真实时长。
- **恢复出来的精简浮窗会被自己撤掉**：浮窗现在能跨重启恢复，而"音乐会话结束就
  自动还原"的判断此前写成"没有音乐在放就还原"——冷启动那一瞬本来就没有音乐在
  放，于是恢复出来的浮窗第一帧就被撤销。现在只在**会话真正结束**（从有到无，
  含视频接管播放器）时才还原。
- **频谱一直是一条直线**：拾音器的 `AnalyserNode` 建了却从来没接进音频图
  （实际链路漏了它），读到的恒是静音——频域全 0、时域全 128，可视化只能走
  "无真实数据"的静态进度线。现在链路补回
  `MediaElementSource → Analyser → 均衡器 → 音量 → 输出`，内置引擎播放的音轨
  能出真实频谱与波形（均衡器同样一直没经过它，一并生效）。
- **热重载后小窗口里画出完整界面**：窗口是浮窗/竖屏尺寸时热重载，窗口尺寸由
  主进程保持、渲染层的状态却归零，于是 400×300 的浮窗里画出带侧栏的完整影视
  界面。现在渲染层启动时会向主进程问一次当前窗口形态并回填。

## 1.3.0（音乐模式隔离、无边框窗口与服务器音乐真频谱）

### 新增
- **无边框窗口**：去掉系统标题栏，改成一条自绘的细标题栏（可拖动、双击最大化，
  右侧最小化 / 最大化 / 关闭）。Linux 的无边框窗口没有系统边框可拖，所以窗口
  四边与四角都加了自绘缩放热区，手感与原生一致；关闭按钮即退出应用。
- **影视模式 / 音乐模式彻底分开**：默认打开就是影视模式，需要听歌时点侧栏
  底部一个不太起眼的「音乐模式」小按钮（再点「返回影视」回来）。切到音乐模式
  后导航项、设置页、媒体库整组换成音乐那一套——音乐界面里不再出现"本地影视/
  搜索/历史/刮削任务"，影视界面里也不再出现音乐入口。两种模式共用同一套播放
  内核，播放中的音乐在切模式后照常继续（迷你条与精简浮窗按音乐会话决定是否
  显示，不按页面决定）。每次启动都回到影视模式。
- **音乐库与影视库各管各的**：一个来源只能属于一域（音乐或影视），不再支持
  混放——视频扫描要读 NFO 按剧集/电影归类，跟音乐混在一个目录里两边效果都差。
  影视模式的「媒体库」管媒体服务器与影视来源，音乐模式的「音乐媒体库」只管
  音乐来源（本地目录 / WebDAV），扫描按域走：影视来源只索引视频与 NFO，音乐
  来源只索引音频与分轨文件。老库升级后存量来源按影视来源处理，已扫出的音乐
  索引不会被删，音乐目录需要在音乐模式重新添加一次。
- **服务器/WebDAV 音乐也有真频谱了**：此前这些音源一律由 mpv 解码，频谱图只能
  如实提示"无法显示真实频谱"。现新增主进程认证流代理（`qy-stream://`），服务器
  （Jellyfin/Emby）与 WebDAV 的直连可解码格式（mp3/aac/flac/ogg/opus/wav 等）
  改走内置引擎——真频谱、真波形、均衡器与本地文件完全一致；token 留在主进程，
  不再拼进渲染层可见的直链。mpv 仍负责转码、CUE、冷门格式与"兼容性优先"。
- **服务器音乐的进度与续播不再因换引擎丢失**：内置引擎播放的服务器曲目照常向
  服务器回传播放会话与进度（起播 Sessions/Playing、播放中 Progress、收尾
  Stopped），跨设备续播与服务器"已播"状态保持同步；本地续播键一并落库，离线
  也能续播。

### 变更
- **导航与设置按模式重组**：设置页页签随模式变化——影视模式是「播放 /
  插件 / 快捷键」，音乐模式是「音乐 / 快捷键」，音乐配置（解码引擎 / 均衡器 /
  ReplayGain / 拾音器 / 歌词 / 睡眠定时）只出现在音乐模式；「快捷键」是应用级配置，
  两种模式共用同一份内容。音乐模式的媒体库入口指向新的音乐来源页（页内只列
  音乐来源，媒体服务器仍在影视模式配置）。
- **引擎判定收敛**：来源类型（本地/服务器/WebDAV）不再强制选择引擎，改为按
  转码/CUE/兼容性优先/格式可解码性判定；服务器条目读不到编解码信息时优先尝试
  内置引擎（真解不了自动回退 mpv，代价只是一次短暂切换）。
- **性能保护**：系统 CPU 紧张时自动降低频谱刷新，把 CPU 让给音频解码，减少播放
  卡顿。主进程按 `loadavg/核心数` 每 3 秒采样压力档（正常/较忙/繁忙），渲染层据此
  把频谱图帧率从 30fps 降到 12/3fps，压力回落后自动恢复；只影响画面刷新，不碰播放。
  精简浮窗右侧的仪表按钮可随手开关，也可在「设置 → 音乐」里切换（默认开启）。
- **精简模式浮窗**：播放音频时可把窗口缩成屏幕右上角的小浮窗（主窗口原地缩小，
  非另开窗口），界面只留频谱图 + 进度（可拖动）+ 上一曲/暂停/下一曲 + 循环模式 +
  随机 + 音量 + 一键还原；迷你条点「精简」手动进入，设置里可开「播放音频时自动
  进入」；音乐会话结束会自动还原。因为与主界面是同一个 renderer，播放状态与
  30fps 频谱零跨进程同步（对老机最省 CPU）。
- **播放频谱图**：音乐页顶部内嵌一块实时频谱区，播放音乐时常驻、随音调跳动，
  支持**柱状**（实时频率分布）与**瀑布**（频率随时间的滚动热力图）两种图表切换，
  选择持久化。真数据来自内置引擎（Web Audio）的 AnalyserNode；仅转码 / CUE /
  冷门格式等仍经 mpv 解码的音源在 mpv 0.32 下没有实时频谱接口，面板会如实提示
  "无法显示真实频谱"，绝不画假跳动。

### 变更
- **设置页按板块分页签**：影视播放（自动连播 / 跳片头片尾）、音乐（解码
  引擎 / 均衡器 / ReplayGain / 拾音器 / 歌词 / 睡眠定时）、插件各自独立成
  一块，一次只显示一块，找音乐配置不再需要滚过影视设置。

### 修复
- **加载失败不再被当成"没有内容"**：观看历史、音乐库、歌单加载失败时只弹一个
  Toast，页面却落成「暂无记录 / 空网格」，看着像数据真的空的。现在失败会显示
  可重试的错误提示，加载中给骨架屏（音乐库此前是一块白屏）。
- **进度条可以用键盘操作**：播放进度条此前只能用鼠标点（键盘完全跳不过去），
  现在能 Tab 聚焦，左右方向键 ±5 秒（按住 Shift ±30 秒）、Home/End 到头尾；
  历史记录里"鼠标悬停才出现"的播放/删除按钮在键盘聚焦时也显形。
- **焦点样式统一**：搜索框、插件设置等处自造的 `focus:border/ring` 收敛到
  项目统一的 `focus-ring`，Tab 一圈的观感一致。
- **页面标题层级修正**：音乐页与歌单页补上唯一的 h1，媒体浏览/详情页的
  「季与集 / 演职员 / 季」从跳级的 h3 改为 h2（读屏与大纲不再错位）。
- **WebDAV 音乐的续播从未生效**：解析播放地址时误读 'local' 域的进度键，而
  WebDAV 进度实际落在 `<来源id>:<路径>` 键下；已改为读对键，mpv 与内置引擎
  两条路径的续播键保持一致。
- **内置引擎回退传给主进程的"强制 mpv"参数被丢弃**：解码失败后的 mpv 兜底
  重新解析时仍会拿到内置引擎结果，回退可能拿错误地址二次失败；现按要求透传。
- **内置引擎解不了、但 mpv 能解的文件现在能播了**：内置引擎解码失败后的
  mpv 兜底此前形同虚设——兜底读的是 store 里的当前曲目，而该状态是在
  `await playQueue()` 之后才写入的，失败时它还是空（首播直接放弃）或是
  上一首（把上一首喂给 mpv）。典型受害文件：FLAC 内嵌图片块的
  `picture.type` 非法（-1）时 Chromium 打开容器即失败，而 mpv 只当警告。
  现在兜底按「失败的那一首」重播，且已交给兜底的那次失败不再弹出误导性
  的错误提示。
- **播放失败不再是「点了没反应」**：音乐页/服务器音乐/服务器歌单读的是
  点击那一刻闭包里的 store 快照，await 期间写下的错误信息永远读不到 →
  失败时没有任何提示。改为读实时状态，失败会给出中文 Toast。
- **内嵌封面为 JPEG 时专辑/歌手网格只显示占位图**：渲染层按
  `<trackId>.png` 请求封面，而落盘名按内嵌图片的真实格式（JPEG 占真实
  世界绝大多数）是 `<trackId>.jpg`，协议层精确匹配 → 请求落空。现按
  「原名 → 同名其他已知扩展名」解析（`resolveCoverFileName`），目录包含
  校验仍在协议层。此前测试夹具三张封面全是 PNG，恰好只覆盖了唯一能命中的
  分支，故一并补上 JPEG 用例。
- **需认证的 WebDAV 音频静默播不出**：音乐 loadfile 漏传认证头会话 id
  （`streamSessionId`），WebDAV 直链不带凭据，mpv 因此拿不到 Basic 认证头
  且失败不上报。`playQueue` / `playServerAt` / 内置引擎回退三处全部补上
  透传。
- **本地音轨（mp3/flac 等）拾音器不随音调跳**：内置引擎在起播路径
  （`playCurrent`）从不调 `AudioContext.resume()`，而该调用不在用户手势的
  同步栈内（`playQueue` 之前有 `resolvePlayback` 的 IPC await），浏览器自动
  播放策略下构造出的上下文停在 `suspended`——整条图
  （source→analyser→…→destination）不运转，既听不到声音、AnalyserNode 也只
  读到全 0，频谱静止。现起播前主动 resume。mpv 引擎（服务器/WebDAV/冷门格式）
  仍无真实频谱，降级波形改为随播放节拍轻微起伏，至少「活」起来。
- **音乐经 mpv 解码时弹出黑屏窗口**：mpv 由 `playerLoadFile` 按媒体类型决定是否
  开窗——视频保持窗口，音乐关掉视频轨（`vid=no`）并撤销强制窗口
  （`force-window=no`）。音频文件常带内嵌封面，mpv 把它当成一条 video 轨
  （mjpeg），即便不改 `force-window` 也会开窗，故必须同时关掉视频轨。该 mpv
  实例是懒启动的（首次播放才起），所以连走内置引擎（webaudio）的音乐也会顺带
  启动它——音乐路径一律压窗，不能只压 mpv 直解的那一类。启动参数由
  `--force-window=immediate` 改为 `--force-window=no`（视频分支再显式置
  `yes`），避免音乐首发时先闪一下黑窗再压掉。
- **拾音器波形是假的，换成真波形**（QYP3-033）：此前 waveform 模式画的是按
  确定性包络 + 正弦相位生成的假波形，与音频无关。现内置引擎经 AnalyserNode
  时域数据（`getByteTimeDomainData`）提供**真实波形**；无真实数据时（mpv 引擎 /
  静音）改为画一条静态进度线，绝不画假跳动。mpv 0.32 没有暴露实时频谱/波形的
  IPC 接口（`audio-fft` 是 0.34+ 才有），升级 mpv 会破坏老系统兼容，故
  服务器/WebDAV/CUE 等走 mpv 的音源仍无法提供真实波形。
- **本地非法封面 FLAC 现在能出真波形、且不再兜底到 mpv**（QYP3-033）：像
  `嘲笑.flac` 这类 `METADATA_BLOCK_PICTURE` 块损坏（`picture.type=-1`）的文件，
  Chromium 在打开容器阶段就整文件失败（此前只能兜底 mpv、既无真波形又弹黑窗）。
  现解码失败时先剥离内嵌封面（`flac-strip.ts` 移除所有 type=6 块、音频帧原样
  保留、重封装成 blob）在内置引擎重播——保留真频谱/真波形，也顺带避开了 mpv
  黑窗。封面展示走 covers 缓存分区，与播放流内嵌封面无关，不受影响。渲染层
  CSP 的 `media-src` 需放行 `blob:`（否则剥离后的 blob 会被 CSP 拒载）。
- **播放本地 FLAC 每次都弹英文报错 "Failed to load because no supported source
  was found."**：QYP3-033 引入封面剥离自救后，`onError` 不再同步置
  `directFallbackTrackId`，于是 `playQueue` 的 catch 把紧随 `error` 事件而来的
  `play()` rejection 当成失败抛了出来——即便自救随后成功。现在自救期间该曲目的
  rejection 被认领（`flacRecoveringTrackId`），不再误报；无自救/兜底认领的真实
  解码失败统一中文化为「这首曲目无法解码播放」（浏览器英文原文不再直接亮给用户）。
- **首个 loadfile 偶发 `ECONNREFUSED mpv-*.sock`**：`MpvProcessManager.start`
  只轮询 socket 文件是否存在，而文件由 `bind()` 创建、`listen()` 之后才可连接，
  两者之间 connect 会被拒。`MpvIpcClient.connect` 现做有界重试（默认约 1.2s），
  只吞连接建立前的错误；真正断开仍照常派发 `disconnect`。

## 1.2.0（三期：音乐播放）

### 新增
- **音乐库**：本地 / WebDAV 音频扫描入库；自研零依赖标签解析
  （ID3v2.3/2.4、FLAC、m4a、APEv2 只读）、内嵌封面提取（covers 分区）、
  CUE 分轨（strict 校验）；「音乐」页专辑网格 / 专辑曲目 / 全部曲目。
- **双播放引擎**（ADR-0007）：引擎选择器按格式与偏好判定——常见格式走
  内置引擎（Web Audio：均衡器 + 实时频谱），服务器 / WebDAV / 转码 /
  CUE / 冷门格式走 mpv（兼容性优先）；解码失败自动回退 mpv 一次。
- **歌单**：内部歌单 CRUD + 排序，m3u/m3u8 导入（相对路径解析 + 未定位
  报告）、m3u8 / XSPF 导出。
- **服务器音乐库**：音乐页可切换到 Jellyfin / Emby 的音乐视图，专辑网格 →
  曲目列表 → 播放（走 mpv 引擎）；播放队列在服务器曲目间上下曲切换。服务器
  曲目不落本地库（以服务器为准，避免与扫描音轨双份）。
- **歌词**：扫描期内嵌歌词落盘（lyrics 受保护分区）；LRC 解析器（标准 /
  增强逐字 / 多时间标签 / 偏移）；歌词面板（同步高亮、点击跳转、手动导入
  .lrc）；**桌面歌词浮窗**（透明置顶、鼠标穿透、解锁可拖动、逐字渐变
  高亮、位置/字号持久化，Wayland 会话明确不可用）。
- **音频链**：10 段均衡器（7 预设，双引擎各一份映射）、ReplayGain（模式 +
  预增益 + 无标签曲目兜底增益 + 削波保护）、`--gapless-audio=weak`、
  全局迷你控制条与媒体键双用途。
- **拾音器**：实时频谱（AnalyserNode fftSize 2048，≤30fps）与播放波形
  （mpv 引擎降级路径），模式可切换。
- **睡眠定时**：到点暂停播放（音乐与视频通用），设置页可选 15 分钟～2 小时
  或关闭，迷你控制条显示剩余倒计时并可一键取消；到点同时取消可能正在
  倒计时的自动连播。
- **服务器歌单（只读）**：歌单页新增「服务器歌单」页签，浏览并播放服务器
  上已有的歌单（按 serverId 严格路由，视频歌单不混入音轨队列）；服务器
  歌单的增删改仍在服务器端进行。
- **mpv 引擎音乐控制条**：音乐迷你条对两个引擎都常驻（此前仅内置引擎），
  mpv 曲目的进度、时间、上下曲、收藏与歌词入口齐备；mpv 自然结束后按
  服务器队列自动续播下一曲。**音乐与视频互斥**：视频起播会结束音乐会话
  （此前内置引擎的音乐会与视频同时出声、控制条残留在画面上）。
- **服务器歌词**：Jellyfin 10.9+ `/Audio/{id}/Lyrics` 接入歌词面板与桌面
  歌词（主进程归一成 LRC，与本地歌词共用同一套解析）；Emby 无该端点，
  显示"服务器上没有这首曲目的歌词"，不报错。

- **音乐库浏览**：专辑 / 歌手 / 全部曲目 / 收藏四个视图；收藏标记随扫描
  音轨持久化，曲目行可直接收藏；本地音乐接入统一搜索。
- **歌单拖拽排序**：条目可拖动调整顺序（保留上/下移按钮作键盘兜底）。
- **收藏快捷键**：默认 `Ctrl+Shift+F`，音乐播放时对当前曲目生效。

### 变更
- 服务器未登录的引导统一指向「媒体库」页（设置页只放软件配置）。
- 目录域新增音乐表（migration 007，追加式）与音乐收藏列（migration 008）；
  歌词/封面走独立缓存分区。

### 修复
- **设置项写进去读不出来**：`SETTINGS.SET` 写 JSON 而 `SETTINGS.GET` 返回
  裸字符串，导致均衡器增益、ReplayGain 模式、自定义 EQ 预设、拾音器「关闭」
  四项功能整体失效（保存显示成功但重启后全部丢失、mpv 收到非法值后静默
  忽略）。读取侧改为与写入侧对称解析，并兼容主进程写入的裸值。

## 1.1.0（二期：续播、插件与统一体验）

### 新增
- **剧集自定义片头/片尾**：详情页「片头/片尾」弹窗按整剧设定（秒），
  优先于服务器识别；Emby/未开启检测的服务器也能用；本地历史合并
  防御服务器同步失败。
- **插件系统**（§11）：内置插件契约 + 静态 registry（apiVersion 1）、
  配置/密钥（write-only）/健康检查 UI、刮削任务引擎（置信度门槛
  0.92/0.75、断点恢复、人工锁字段）、**TMDB 内置插件**（电影/剧集/季/集、
  zh-CN→en-US 回退、密钥仅 Bearer 头）、单项/批量刮削 UI + 任务监控页。
- **豆瓣实验插件契约**（ADR-0006）：数据入口门禁完成；评审签认前
  保持"已内置但不可启用"，UI 零入口零请求。
- **续播单源**（§12）：纯函数 ResumeResolver（30s/90%/看完下一集/重播）；
  剧集主按钮四态文案；单集卡进度与从头播放；可取消自动下一集
  （5s 倒计时、设置开关、手动停止/崩溃/退出不触发、EOF 去重）。
- **剧集跳过片头/片尾**：Jellyfin 10.9+ MediaSegments 自动识别，播进
  片头段自动跳过（默认开）、片尾段默认关；跳到片尾自然衔接自动连播；
  失败/旧版服务器永不影响播放
- **统一首页/搜索**（§16.4）：四类来源（本地/WebDAV/Jellyfin/Emby）的
  继续观看、最近添加、搜索合并；按完整 MediaRef 去重；分页 ≤200；
  来源局部失败不阻塞。
- **技术信息**：一次性 headless mpv probe（0.29/0.32 属性回退）+
  版本化缓存；详情页技术信息面板与进度摘要。
- **元数据编辑器**：字段 provenance、冲突差异、批量原子保存、海报/背景导入。
- **两阶段安全删除**：预览→单次令牌→执行复验；本地仅回收站；
  WebDAV 标题确认 + 412 指纹变化检测；未知状态标记不假成功。
- **字幕导入**：临时文件+原子 rename、20MiB 上限、双向孤儿清理、
  播放注入（mpv 0.29 兼容）。
- **诊断摘要**：脱敏可分享（无密钥/无完整私有 URL/无绝对路径）。
- **WebDAV 播放/seek**：206 探测、NO_CREDENTIAL 降级提示、进度统一 sink。

### 变更
- **服务端进度回传全链修复**：起播报告 Sessions/Playing、收尾走
  Stopped（此前 Emby 拒收 400，服务器侧进度永不更新）；本地历史
  合并兜底——剧集续播按钮不再恒定第一集。
- 单实例锁（重复启动唤起已有窗口）+ 窗口标题随页面内容变化。
- 深色主题文本降亮度（正文 88%/标题 92%/弱化 62%），消除暗场眩光。
- 本地页「最近播放」只显示本地记录；空态指向媒体库页。
- 无 dev-server 启动的加载路径修复（out/renderer）。
- 设置页仅软件配置；媒体服务器/来源迁至「媒体库」页（U-001/002）。
- 首页视觉现代化（U-003）：图标、分段控件、进度条、深色语义 token。
- 缓存配额/预算常量单源：local≤8、WebDAV≤4、probe≤1、scraper≤2、
  事件 ≤4Hz、页 ≤200；人工字幕永不清扫。

### 兼容
- Electron 锁定 21.4.4（Deepin 20.9 / Debian 10 / glibc 2.28）；
  mpv 仅软件解码；数据库 migration 仅追加。
