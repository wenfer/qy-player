# 待目标机验证清单（v1.1.0 安装后手工验证）

机器可验证的门禁（typecheck / 636 tests / 打包产物）已全绿并随 v1.1.0
发布；以下为只能在目标机（Deepin 20.9 / Debian 10 / glibc 2.28）上
手工完成的验证项。全部通过后可勾选归档。

## 安装与启动

- [ ] Deepin 20.9 / Debian 10 安装 deb / AppImage；启动、退出、托盘、快捷键注册
- [ ] 无横向滚动（1280×800：刮削任务页 / 详情页 / 删除对话框 / 片头片尾弹窗）

## 播放（mpv 0.29 与 0.32 各跑一遍）

- [ ] 真实播放、seek、技术信息探测、字幕切换
- [ ] 短视频 EOF 自动下一集（倒计时 / 取消 / 设置关闭 / 最后一集）
- [ ] 剧集续播文案四态（继续播放 · 位置 / 播放下一集 / 播放第 X 集 / 重新播放确认）
- [ ] 跳过片头（Jellyfin 识别）与自定义片头/片尾弹窗设定生效

## 服务器同步

- [ ] Emby / Jellyfin 播放后网页端进度同步（Sessions/Playing 链路）
- [ ] TMDB 真实 key 全链路：配置 → 单项 → 批量 → 确认 → 取消 → 恢复；
      诊断摘要复查无密钥泄漏

## 来源矩阵

- [ ] WebDAV 兼容：Nextcloud / Apache / NAS / Alist（连接、播放、seek
      probe、删除 412/确认、扫描）
- [ ] 混合来源流程：local + WebDAV + Jellyfin + Emby 的继续观看 /
      最近添加 / 搜索

## 音乐（三期，v1.2.0 增补）

- [ ] 本地 / WebDAV 音频扫描：标签（ID3v2/FLAC/m4a）入库、内嵌封面显示、
      CUE 分轨（strict：引用缺失则整张不入库）
- [ ] 封面格式覆盖：**内嵌封面是 JPEG 的专辑/歌手要有图**（此前只有 PNG
      夹具走过，真实 MP3 的 APIC 多为 image/jpeg；修的是这条分支）
- [ ] 需认证的 WebDAV 音频要能出声（此前认证头会话漏传 → mpv 静默 401，
      连错误提示都没有；同一来源的视频播放一直正常，可作对照）
- [ ] 双引擎播放：常见格式（mp3/flac/m4a…）走内置引擎；冷门格式
      （ape/wma/dsf…）走 mpv；曲间 gapless 无爆音（实听）
- [ ] 迷你控制条 / 全局媒体键：上下曲、播放暂停、歌词面板开合
- [ ] 桌面歌词（GNOME/X11）：透明生效（**若黑底则记录为降级项**）、
      置顶、鼠标穿透（锁定）/ 解锁拖动、位置与字号重启后恢复
- [ ] 性能保护（QYP3-036）：播放音乐时用其它程序把 CPU 压满 → 频谱图刷新明显变慢
      （帧率下降但仍在动），压力回落后自动恢复流畅；精简浮窗的仪表按钮与「设置 →
      音乐」的开关都能即时生效，关掉后即使 CPU 满载也不再降帧；关掉开关再制造压力，
      对比确认开着时播放更不卡（降帧确实把 CPU 让给了音频）
- [ ] 精简模式浮窗（QYP3-035）：播放音乐时点迷你条「精简」→ 主窗口缩成屏幕
      右上角置顶小浮窗，显示频谱图 + 进度 + 上一曲/暂停/下一曲 + 循环/随机 + 音量；
      拖动进度可跳转、音量与循环即时生效；点浮窗「还原」恢复原尺寸（原位置/可缩放）；
      设置里开「播放音频时自动进入」后起播自动缩窗；音乐会话结束（或开始放视频）
      自动还原，不把用户困在空小窗；多显示器下缩到窗口所在屏的右上角
- [ ] 拾音器：实时频谱与**真实波形**切换；30fps 下的 CPU 占用采样（老机预算）
- [ ] 播放频谱图（QYP3-034 / QYP3-047 起只有柱状）：音乐页**不再**有顶部频谱图
      （只留底部播放条的拾音器）；本地音轨的经典弹跳柱随音调实时跳动、峰值帽
      缓慢下落；服务器 / WebDAV 等 mpv 音源显示"无法显示真实频谱"提示
      （不画假数据）；30fps 下老机 CPU 可接受（无瀑布图）
- [ ] 拾音器**真波形**（QYP3-033）：本地普通音轨（mp3/正常 flac）在 waveform /
      spectrum 模式都出现随音调起伏的**真实**波形/频谱；嘲笑.flac 这类
      `METADATA_BLOCK_PICTURE` 非法的 FLAC 不再被兜底到 mpv，应在内置引擎出
      **真波形**且不弹 mpv 窗口（非法封面被剥离后重播）；服务器/WebDAV/CUE 等走
      mpv 的音源拾音器显示**静态进度线**（诚实告知无真实波形，绝不计假跳动正弦波）
- [ ] 音乐经 mpv 解码**不得弹出 mpv 视频窗口**（QYP3-032）：本地冷门格式 /
     服务器 / WebDAV / 内置引擎兜底（如内嵌封面非法的 FLAC）的音乐播放时只走
     底部迷你条，不应出现独立 mpv 窗口（音频文件内嵌封面会被 mpv 当成 video
     轨，必须 `vid=no` + `force-window=no` 压窗）；视频播放仍应有 mpv 窗口；
     音乐→视频→音乐切换窗口状态正确
- [ ] 歌单：新建/重命名/删除/排序、m3u8 与 XSPF 导入导出往返
- [ ] 服务器音乐（Jellyfin/Emby）：音乐页来源切换出现服务器的音乐库；
      专辑→曲目→播放出声；播放中上下曲在服务器队列内推进（队尾停止，
      不回卷到第一首）；非音乐库（电影/剧集）与未登录服务器不出现在来源里
- [ ] 引擎切换与互斥：mpv 引擎音乐（服务器曲目 / 冷门格式）下迷你条出现、
      进度条与时间跟随、拾音器为波形；此时播放一个视频 → 音乐条消失、
      没有两路声音同时响；视频播放时迷你条不得出现
- [ ] 服务器曲目播放中按全局媒体键「下一曲」应是队列下一首（不是快进 30 秒）
- [ ] 服务器歌词：Jellyfin 上带歌词的曲目在歌词面板与桌面歌词同步高亮
      （服务器歌词无逐字标签 → 无卡拉OK 渐变属预期）；Emby 曲目显示
      "服务器上没有这首曲目的歌词"；面板对服务器曲目不显示「导入歌词」
- [ ] 服务器歌单：歌单页切到「服务器歌单」应列出已激活服务器上的歌单
      （未激活的服务器不出现）；点开能看到条目、点曲目出声且队列只含
      音轨；界面上找不到任何新建/重命名/删除入口

### 仅当决定实现网络收音机时（见 ADR-0009）

- [ ] 目标机 ffmpeg/mpv 是否把 ICY 标题填进 mpv 元数据（`icy-title`）：
      用一个 Icecast 电台实测，观察 mpv 的 `metadata` 属性
- [ ] 老机 mpv 0.29 断流重连（`--stream-lavf-o=reconnect=1`）的实际表现，
      以及"重连中"对用户是否可见
- [ ] 实现前必须先落地 `playback-state` 的"不可续播媒体"标志，
      验证直播流不会在历史/继续观看里留下记录
- [ ] Wayland 会话下桌面歌词入口应给出不可用提示（不崩溃）
- [ ] 睡眠定时：设置 15 分钟档后迷你条出现倒计时徽标；到点音频停止
      （视频暂停在 mpv 窗口、音乐暂停在迷你条）；播放音乐与播放视频
      两种场景都要试；到点时若正在自动连播倒计时则连播被取消；
      取消定时后不再触发；重启应用后不应残留上次的定时

### qy-stream 认证流代理（QYP3-037/038，本机只能跑单测，实机必须验）

- [ ] 服务器音频（Jellyfin mp3 与 flac 各一）经内置引擎播放：出声、频谱图与
      拾音器为真实数据（不再出现"无法显示真实频谱"提示）
- [ ] 服务器音频 seek：拖动进度条后立即出声（Range/206 经 `stream: true`
      scheme 的实机行为是本任务最大的未知数）；seek 多次不卡死、不静音
- [ ] WebDAV 音频（mp3/flac）同样走内置引擎出真频谱；带密码的 WebDAV 不
      401（认证由主进程代理注入）
- [ ] 服务器/WebDAV 播放中切换曲目/停止后，上游连接被关闭（服务器端无长期
      挂起的连接泄漏；可用 `ss -tnp | grep <服务器IP>` 观察）
- [ ] 服务器 FLAC 含非法内嵌封面时仍能在内置引擎自救（不弹 mpv 黑窗）
- [ ] 服务器音乐跨设备续播：播放一首到中途 → 退出应用 → 手机/网页端看到
      进度已回传；本机重开后从服务器位置续播；播放完整一首后服务器标记已播
- [ ] WebDAV 音乐续播：播放到中途退出再进，能从上次位置续播
      （<sourceId>:<path> 续播键此前从未生效，本次为首次可用）
- [ ] 兜底路径：服务器上冷门格式（如 ape）曲目能自动回退 mpv 播出声（队列
      后续曲目继续推进，不卡死、不重复）；兼容性优先设置下服务器音频全部
      走 mpv 且不弹窗
- [ ] 主进程 stdout 无新增噪音：qy-stream 代理不应逐请求打日志

## 模式隔离与来源按域分离（QYP3-039/040/041，本机只能跑单测，实机必须验）

- [ ] 老库升级（QYP3-041 migration 010）：用升级前建好的库启动，存量来源全部
      变成影视来源（音乐模式「音乐媒体库」里看不到它们），影视侧扫描结果与
      升级前一致；**已扫出的音乐索引一条不少**（升级不删数据）
- [ ] 音乐目录在音乐模式重新添加后，扫描只出音频与分轨（该目录里混着的剧集
      文件不进「首页 / 本地」）
- [ ] 影视来源扫描：NFO 归类正常，目录里的音频文件既不入库也不被误删
      （`cleanupMissingMusic` 在 video 域跳过）
- [ ] 模式切换：点侧栏底部「音乐模式」落到音乐页且导航整组换成音乐项；点
      「返回影视」回到首页；播放音乐中途切模式，声音不中断、迷你条/精简浮窗照常
- [ ] 音乐模式「设置」只出现音乐与快捷键两块，影视模式只出现播放/插件/
      快捷键；快捷键在两种模式下是同一份配置（改一处两边一致）
- [ ] 音乐模式「媒体库」里不出现媒体服务器区块（只有一句"在影视模式管理"的
      提示），服务器音乐仍能在音乐页正常浏览播放
- [ ] 1280×800 目标分辨率下，音乐模式与影视模式都不出现横向滚动条
      （两套导航 + 底部模式入口在窄侧栏里不撑破）

## 无边框窗口（QYP3-042，本机只能跑单测，实机必须验）

- [ ] 标题栏拖动：按住标题栏能把窗口拖走；拖动区里的按钮（最小化/最大化/
      关闭）点击正常，不会被拖动手势吃掉
- [ ] 双击标题栏最大化 / 再双击还原；三个按钮分别最小化、最大化、关闭
      （关闭 = 退出应用，进程不残留）
- [ ] 八向缩放：四边与四角都能拖，右下角拖到最小尺寸后被 1280×800 夹住；
      从左边/上边拖时窗口位置跟着走（被下限夹住时不"跑"）；最大化状态下拖
      热区无效（不报错、不改尺寸）
- [ ] GNOME（X11 与 Wayland 各试一次）与 Deepin 20.9 下的观感：无边框窗口
      是否有阴影/圆角缺失、Alt+拖动与 WM 快捷键（最大化）是否仍可用，
      最大化图标是否随 WM 操作同步
- [ ] 精简浮窗：28px 紧凑标题栏可拖动，「还原窗口」按钮回到正常尺寸；
      Toast 不被标题栏压住
- [ ] 侧栏与内容区在标题栏之下对齐（无 36px 错位、无双滚动条）

## 音乐模式竖窄屏（QYP3-044，本机只能跑单测，实机必须验）

- [ ] 1366×768 与 1920×1080 两种分辨率下：切到音乐模式主窗口变成竖屏且不溢出
      工作区（矮屏高度被夹到 屏高-48），位置居中不压任务栏
- [ ] 从音乐模式切回影视：窗口精确还原成进入前的尺寸与位置（不是被 1280×800
      下限顶变形）；下限也恢复成 1280×800
- [ ] 最大化状态下切音乐模式：能正常取消最大化并变成竖屏（不是卡在最大化）
- [ ] 音乐模式里点迷你条「精简」进浮窗，再点还原：回到**竖屏**而不是正常尺寸；
      此时退出音乐模式才回到正常尺寸
- [ ] 浮窗 / 竖屏状态下热重载（Ctrl+R）：界面跟着窗口形态走（浮窗就是精简
      播放界面、竖屏就是音乐模式图标轨），不会在小窗口里画出带侧栏的完整影视
      界面；重载也不会把已经摆好的窗口重新挪一遍
- [ ] 进音乐模式默认就是「全部曲目」列表（不用再点 tab），tab 顺序是
      全部曲目 / 专辑 / 歌手 / 收藏
- [ ] 放歌时播放控件停靠在音乐窗口底部（不是居中浮卡）：上一曲 / 播放暂停 /
      下一曲 / 进度 / 歌词 / 收藏 / 精简都在，竖屏下换行而不是横向溢出；
      列表最后几行不会被这条挡住
- [ ] 竖屏（约 460×820）下音乐页/歌单页/音乐设置/音乐媒体库都不出现横向滚动条，
      侧栏图标轨能看出当前页（选中态可辨）

## 频谱/波形（QYP3-045 修复，本机无音频设备，必须实机看）

- [ ] 本地 mp3（内置引擎）播放：拾音器柱状频谱随音乐跳动，不是一条直线；
      切到波形模式也是真实起伏
- [ ] 均衡器调一档后频谱立刻变化（链路经过 EQ，说明取样点在图上）
- [ ] mpv 引擎的音源（转码 / CUE / 兼容性优先 / 冷门格式）仍然如实显示静态
      进度线，不假跳动——这是设计如此，别当成回归
- [ ] 静音/暂停时频谱归零不再乱跳

## 频谱部件合并与经典弹跳柱（QYP3-047，本机无音频设备，必须实机看）

- [ ] 播放音乐时**只出现一个频谱**：底部播放条（音乐模式里停靠在窗口底部）
      的那条拾音器；音乐页顶部不再有第二块频谱图
- [ ] 频谱是"经典弹跳"手感：分段 LED 柱（底段琥珀→顶段红）随音乐起伏，顶端
      峰值帽在电平掉下去后**缓慢下落**再回到底部（不是跟着瞬间归零）
- [ ] 24px 高的拾音器里分段仍看得清（老机分辨率下不糊成一团）
- [ ] 精简浮窗与音乐页里都不再有「瀑布声谱图」入口（设置里也找不到图表选择）
- [ ] mpv 引擎的音源（转码 / CUE / 兼容性优先 / 冷门格式）：拾音器仍是静态
      进度线、频谱图仍是"无法显示真实频谱"提示，绝不假跳动

## 频谱加高与显隐开关（QYP3-048，实机看观感）

- [ ] 播放条里的频谱高度是 32px：LED 分段在 1366×768 下分得清；停靠态（音乐
      模式竖屏）下最后几行曲目仍不被播放条挡住
- [ ] 控制条上的频谱按钮：点一下频谱收起（不画空白、CPU 也停下来），再点
      回来；按钮高亮态能看出当前是开是关
- [ ] 关掉频谱后重启应用，仍是关着的；播放中切模式 / 进精简浮窗不把状态搞丢

## 播放条重新排版（QYP3-049，实机看手感）

- [ ] 停靠态（音乐模式竖屏）播放条是四行：频谱 / 曲名+时间 / 进度条 / 按钮；
      窄到 380px 时按钮换行而不是被挤成椭圆
- [ ] 进度条能点能拖：点中间跳到中间、点末尾跳到末尾；拖动跟手且不刷屏
- [ ] 进度条键盘可用：Tab 聚焦有焦点环，←/→ 快退快进 5 秒（Shift 30 秒），
      Home/End 到头尾
- [ ] **暂停时频谱不消失**：冻结在最后一帧（不再是"变成一根进度条"）；继续播放
      后恢复跳动
- [ ] 正常播放时峰值帽不再每秒被打回当前电平（进度走动不该重置可视化）
- [ ] mpv 音源在**没有可用频谱**时（无 ffmpeg，或这条曲目算不出）：频谱位置是
      一根静音底线，**不画进度**（进度只在进度条那一行）；不假跳动
- [ ] 音乐页列表最后几行不被四行播放条挡住

## mpv 音源的离线频谱（QYP3-050，本机只能跑单测+真实 ffmpeg 半集成）

本机已用**真实 APE（5:21）+ 真实 mpv** 验证过时间轴对齐（结论见下），所以
下面只留"只能在目标机上看"的观感 / 性能 / 降级项，不必再验同步：

- 独立 `ffmpeg -ss 120` 解出的片段与全曲矩阵在偏移 0 处逐帧完全一致
  （平均绝对差 0.0），偏移 ±1 帧即明显分叉 → 矩阵时间轴就是文件绝对时间轴
- 真实 mpv：`time-pos` 起播 0.076s、推进 0.9987× 墙钟；`duration` 321.668s
  vs 矩阵 321.667s（差 1ms）
- 渲染层 1Hz 锚点 + 线性外推的取帧误差 ≤ 1 帧（≈83ms）且帧号不回退

- [ ] **有 ffmpeg 时**：播一首非直解格式（本地 APE / WebDAV 的冷门格式 / Emby 上
      codec 非直解的曲目）→ 起播后几秒内底部的频谱开始跳动（不再是静音底线），
      暂停时冻结在最后一帧，seek 后频谱与声音重新对齐
- [ ] 老机 CPU：解码期间播放不卡顿、不掉帧（并发 1、起播 3s 后才开工）；
      一首 5 分钟的歌后台解码应在十几秒内结束（本机实测 ~1s，老机按 10-20× 估）
- [ ] 缓存：`~/.config/qy-player/music-spectrum/` 出现 `*.qys`（一首 4 分钟 ≈138KB）；
      诊断摘要里能看到 `music-spectrum` 分区；删掉目录后下次播放会重算
- [ ] **没有 ffmpeg 时**（把 PATH 里的 ffmpeg 藏起来，或目标机本就没有）：
      频谱回到静音底线，别的功能一切正常，没有报错弹窗、主进程 stdout 无噪音
- [ ] 设置 → 音乐「mpv 音源频谱」：关掉后不再生成、也不再读缓存；重启保持
- [ ] 服务器/WebDAV 曲目走这条路时：认证由主进程注入（不应出现 401），
      播放中途换曲目时旧的 ffmpeg 进程立即退出（`ps` 里看不到残留）
- [ ] 退出应用时 ffmpeg 不残留（`will-quit` 里同步 SIGKILL）
- [ ] 超长曲目（有声书 >2 小时）直接跳过、不生成缓存、不卡播放

## 曲目时长补全（QYP3-052，本机已用真实库副本验证扫描期）

本机实测（真实库副本 + 真实 ffmpeg 对照）：214 首 / 67 首缺时长 → **扫一次后
210 首 / 只剩 6 首**；解析成功的 58 首与 ffmpeg 的时长误差为 0（首帧即 Xing/Info
总帧数）。剩下 6 首是没有 Xing 头的 VBR 与标签超大的 mp3，靠播放回填。

- [ ] 清掉 `app_config` 里的 `music.durationScanVersion`（或从没扫过）后重扫音乐
      库：mp3/APE 的时长应成批出现，扫描耗时可接受（只多读这些文件的头部 512 KiB）
- [ ] **再扫一次**：解析不出时长的文件不应被重复读取（版本闸门只在解析器升级时开）
- [ ] 挑一首仍然空白的曲目播放一遍 → 回到列表（重进该页）时长出现；再播一遍不该
      重复写库
- [ ] WebDAV 来源的曲目：扫描后仍为空（本轮不接 `readAudio`），播放一次后补上
- [ ] 服务器（Jellyfin/Emby）曲目：时长走 `RunTimeTicks`，不受本次改动影响

## 窗口与模式记忆（QYP3-051，实机看行为）

- [ ] 影视模式下调好窗口大小/位置，关掉再开 → 原样回来
- [ ] 最大化状态下退出 → 下次启动仍是最大化；先 `unmaximize` 再退出 →
      下次是那个还原尺寸，不是"最大化大小但没最大化"的怪状态
- [ ] 切到音乐模式（竖窄屏）直接退出应用 → 下次启动就是竖窄屏音乐模式
      （不需要手动再切一次）
- [ ] 精简浮窗里退出应用 → 下次启动就是右上角小浮窗（显示"未在播放"），
      点「还原窗口」能回到上次的正常几何
- [ ] 音乐模式里开浮窗再退出应用 → 下次启动是浮窗，还原后回到**竖窄屏**
      （不是在浮窗里的 400×300）
- [ ] 音乐模式下手动改过尺寸 → 退出再开仍是标准 460×820（音乐模式尺寸不记忆）
- [ ] **拔掉副显示器**（或把分辨率调小）后启动 → 窗口仍在可见区域内，
      不会跑到屏幕外（至少露出标题栏）；改回来仍正常
- [ ] 目标机 WM 上"隐藏窗口期间 `maximize()`"是否生效（个别 WM 会忽略）
- [ ] 记忆损坏（手工把 `window.memory` 改成乱码）→ 启动回落主屏居中 1600×900，
      不报错、不白屏

## 音乐的"当前播放状态"（QYP3-053，实机看行为）

本机已用**真实库副本**验证过迁移（停 010 的库，28 条历史 / 27 条进度 → 各剩 6 条；
22 / 21 条音频行全部清除，被清的都是曲目名，保留的都是影视与服务器行），
所以下面只留目标机上的观感项。

- [ ] 放一首歌（本地 / WebDAV / 服务器都试），退出应用再打开 → 播放条**出现在
      上次那首与进度上，但不出声**；点播放键从上次位置继续（±5s 内可接受）
- [ ] **历史页不再出现音乐**；首页「继续观看」里也没有音乐（升级后存量音乐记录
      被清掉；影视记录/继续观看影视一切照旧）
- [ ] 点同一首歌（列表 / 专辑 / 歌单）→ **从头播**，不接上次位置
- [ ] 服务器曲目（Jellyfin/Emby）：播放中在服务器的会话/播放进度照常更新；
      换设备看到的「继续收听」位置与服务器一致（本地不记历史不影响回传）
- [ ] 恢复态下按全局媒体键（播放/暂停、上一曲/下一曲）**不生效**（此时没有会话）；
      点播放条上的播放键起播后，媒体键恢复正常
- [ ] 上次退出时在**精简浮窗**里 → 重启后浮窗标题位显示上次那首，点播放能起播
- [ ] 升级安装（旧库有音乐历史）：打开后历史页的音乐条目消失，影视条目一条不少
- [ ] `app_config` 里的 `music.nowPlaying` 手工改成乱码 → 启动不报错、播放条不显示

## 待人工决策（非安装项）

- [ ] ADR-0006（豆瓣数据入口）三方评审签认——签认前豆瓣保持不可启用
