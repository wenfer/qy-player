# 待目标机验证清单（v1.1.0 安装后手工验证）

机器可验证的门禁（typecheck / 636 tests / 打包产物）已全绿并随 v1.1.0
发布；以下为只能在目标机（Deepin 20.9 / Debian 10 / glibc 2.28）上
手工完成的验证项。全部通过后可勾选归档。

## 安装与启动

- [ ] Deepin 20.9 / Debian 10 安装 deb / AppImage；启动、退出、托盘、快捷键注册
- [ ] 无横向滚动（1280×800：刮削任务页 / 详情页 / 删除对话框 / 片头片尾弹窗）

## 播放（mpv 0.29 与 0.32 各跑一遍）

- [ ] 真实播放、seek、技术信息探测、字幕切换
- [ ] 短视频 EOF 自动下一集（倒计时 / 取消 / 设置关闭 / 最后一集）
- [ ] 剧集续播文案四态（继续播放 · 位置 / 播放下一集 / 播放第 X 集 / 重新播放确认）
- [ ] 跳过片头（Jellyfin 识别）与自定义片头/片尾弹窗设定生效

## 服务器同步

- [ ] Emby / Jellyfin 播放后网页端进度同步（Sessions/Playing 链路）
- [ ] TMDB 真实 key 全链路：配置 → 单项 → 批量 → 确认 → 取消 → 恢复；
      诊断摘要复查无密钥泄漏

## 来源矩阵

- [ ] WebDAV 兼容：Nextcloud / Apache / NAS / Alist（连接、播放、seek
      probe、删除 412/确认、扫描）
- [ ] 混合来源流程：local + WebDAV + Jellyfin + Emby 的继续观看 /
      最近添加 / 搜索

## 音乐（三期，v1.2.0 增补）

- [ ] 本地 / WebDAV 音频扫描：标签（ID3v2/FLAC/m4a）入库、内嵌封面显示、
      CUE 分轨（strict：引用缺失则整张不入库）
- [ ] 封面格式覆盖：**内嵌封面是 JPEG 的专辑/歌手要有图**（此前只有 PNG
      夹具走过，真实 MP3 的 APIC 多为 image/jpeg；修的是这条分支）
- [ ] 需认证的 WebDAV 音频要能出声（此前认证头会话漏传 → mpv 静默 401，
      连错误提示都没有；同一来源的视频播放一直正常，可作对照）
- [ ] 双引擎播放：常见格式（mp3/flac/m4a…）走内置引擎；冷门格式
      （ape/wma/dsf…）走 mpv；曲间 gapless 无爆音（实听）
- [ ] 迷你控制条 / 全局媒体键：上下曲、播放暂停、歌词面板开合
- [ ] 桌面歌词（GNOME/X11）：透明生效（**若黑底则记录为降级项**）、
      置顶、鼠标穿透（锁定）/ 解锁拖动、位置与字号重启后恢复
- [ ] 拾音器：实时频谱与播放波形切换；30fps 下的 CPU 占用采样（老机预算）
- [ ] 音乐经 mpv 解码**不得弹出 mpv 视频窗口**（QYP3-032）：本地冷门格式 /
     服务器 / WebDAV / 内置引擎兜底（如内嵌封面非法的 FLAC）的音乐播放时只走
     底部迷你条，不应出现独立 mpv 窗口（音频文件内嵌封面会被 mpv 当成 video
     轨，必须 `vid=no` + `force-window=no` 压窗）；视频播放仍应有 mpv 窗口；
     音乐→视频→音乐切换窗口状态正确
- [ ] 歌单：新建/重命名/删除/排序、m3u8 与 XSPF 导入导出往返
- [ ] 服务器音乐（Jellyfin/Emby）：音乐页来源切换出现服务器的音乐库；
      专辑→曲目→播放出声；播放中上下曲在服务器队列内推进（队尾停止，
      不回卷到第一首）；非音乐库（电影/剧集）与未登录服务器不出现在来源里
- [ ] 引擎切换与互斥：mpv 引擎音乐（服务器曲目 / 冷门格式）下迷你条出现、
      进度条与时间跟随、拾音器为波形；此时播放一个视频 → 音乐条消失、
      没有两路声音同时响；视频播放时迷你条不得出现
- [ ] 服务器曲目播放中按全局媒体键「下一曲」应是队列下一首（不是快进 30 秒）
- [ ] 服务器歌词：Jellyfin 上带歌词的曲目在歌词面板与桌面歌词同步高亮
      （服务器歌词无逐字标签 → 无卡拉OK 渐变属预期）；Emby 曲目显示
      "服务器上没有这首曲目的歌词"；面板对服务器曲目不显示「导入歌词」
- [ ] 服务器歌单：歌单页切到「服务器歌单」应列出已激活服务器上的歌单
      （未激活的服务器不出现）；点开能看到条目、点曲目出声且队列只含
      音轨；界面上找不到任何新建/重命名/删除入口

### 仅当决定实现网络收音机时（见 ADR-0009）

- [ ] 目标机 ffmpeg/mpv 是否把 ICY 标题填进 mpv 元数据（`icy-title`）：
      用一个 Icecast 电台实测，观察 mpv 的 `metadata` 属性
- [ ] 老机 mpv 0.29 断流重连（`--stream-lavf-o=reconnect=1`）的实际表现，
      以及"重连中"对用户是否可见
- [ ] 实现前必须先落地 `playback-state` 的"不可续播媒体"标志，
      验证直播流不会在历史/继续观看里留下记录
- [ ] Wayland 会话下桌面歌词入口应给出不可用提示（不崩溃）
- [ ] 睡眠定时：设置 15 分钟档后迷你条出现倒计时徽标；到点音频停止
      （视频暂停在 mpv 窗口、音乐暂停在迷你条）；播放音乐与播放视频
      两种场景都要试；到点时若正在自动连播倒计时则连播被取消；
      取消定时后不再触发；重启应用后不应残留上次的定时

## 待人工决策（非安装项）

- [ ] ADR-0006（豆瓣数据入口）三方评审签认——签认前豆瓣保持不可启用
